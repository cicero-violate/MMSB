*** Begin Patch
*** Add File: mmsb-service/src/services/proposer_service.rs
+use crate::{Service, ServiceContext};
+use mmsb_events::Intent;
+use std::future::Future;
+use std::pin::Pin;
+
+/// ProposerService
+/// Originates intents and submits them through the JudgmentBus.
+pub struct ProposerService {
+    name: String,
+}
+
+impl ProposerService {
+    pub fn new() -> Self {
+        Self { name: "proposer".into() }
+    }
+
+    pub fn submit_intent(&self, ctx: &dyn ServiceContext, intent: Intent) {
+        let jb = ctx
+            .judgment_bus()
+            .expect("ProposerService requires JudgmentBus");
+
+        let created = jb.submit_intent(intent);
+        let policy = jb.evaluate_policy(created);
+
+        if let Some(judgment) = jb.exercise_judgment(policy) {
+            jb.request_admission(judgment);
+        }
+    }
+}
+
+impl Service for ProposerService {
+    fn name(&self) -> &str {
+        &self.name
+    }
+
+    fn run(&mut self) -> Pin<Box<dyn Future<Output = ()> + Send>> {
+        Box::pin(async {})
+    }
+}
*** End Patch
