[package]
name = "mmsb-policy"
version = "0.1.0"
edition = "2021"

[lib]
name = "mmsb_policy"
path = "src/lib.rs"

[features]
default = []
test-helpers = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
glob = "0.3"

[dev-dependencies]
